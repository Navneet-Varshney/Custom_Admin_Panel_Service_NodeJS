/**
 * Constants for ID prefixes
 */
const adminIdPrefix = "ADM";
const userIdPrefix = "USR";
const clientIdPrefix = "CLT";

module.exports = {
    adminIdPrefix,
    userIdPrefix,
    clientIdPrefix
}